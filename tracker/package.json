{
  "name": "intern-progress-tracker",
  "version": "1.0.0",
  "description": "Daily progress tracker for interns with secure authentication",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "mongodb": "^6.3.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
